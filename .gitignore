node_modules/
dist/
.env

# Claude Code 相关文件（不上传到 GitHub）
CLAUDE.md
.claude/

# 本地集成测试与测试输出（不上传到 GitHub）
test-*.js
*.log

# LLM 调用留痕（本地调试产物，含完整对话内容）
# 用 traces* 覆盖实验目录（如 traces-compress-test/），避免误提交
traces*/

# Python 沙箱工作区与浏览器 profile（本地运行产物）
# profile 里的 cookie 等价于活凭证，绝不能进版本库
sandbox/
.browser-profile/

# 沙箱 venv（模型装的包落在这里，碰不到全局环境）。
# 放在工作区之外是刻意的：放进去模型的代码就能改 venv 自身
.sandbox-venv/
